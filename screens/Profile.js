import {View, StyleSheet, Text, ScrollView} from 'react-native';
import NewList from '../components/new_list';
import Constants from 'expo-constants';

export default function Profile(){
    return(
        <View>
            <Text>Aqui ta tu perfil</Text>
        </View>
    );
}